services:
  docs:
    image: doi_portal_local_docs
    container_name: doi_portal_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./doi_portal:/app/doi_portal:z
    ports:
      - '9000:9000'
    command: /start-docs
